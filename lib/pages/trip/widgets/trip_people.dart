import 'package:flutter/material.dart';

class TripPeople extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: new Text('People'),
    );
  }
}