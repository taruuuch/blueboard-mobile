import 'package:flutter/material.dart';

class TripParticipant extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: new Text('People'),
    );
  }
}